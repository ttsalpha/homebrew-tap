# ttsalpha Tap

## How do I install these formulae?

`brew install ttsalpha/tap/<formula>`

Or `brew tap ttsalpha/tap` and then `brew install <formula>`.

Or, in a `brew bundle` `Brewfile`:

```ruby
tap "ttsalpha/tap"
brew "<formula>"
```

## Documentation

`brew help`, `man brew` or check [Homebrew's documentation](https://docs.brew.sh).
